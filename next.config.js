/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "images.pexels.com",
            "cloudinary.com"
        ]
    }
};



module.exports = nextConfig;
